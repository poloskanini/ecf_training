{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

{% for user in users %}
L'adresse mail de contact du client {{ user.name }} est {{ user.email }} . Ce mail est rattaché au partenaire {{ user.partner.name }}.
Ses droits sont les suivants :
<ul>
    {% for permission_object in user.partner.permissions %}
    <li>
      {% for key, value in permission_object %}
          {{ key }}  {{ value }}
        {% endfor %}
       </li>
    {% endfor %}

</ul>

    
<br>
{% endfor %}        
</div>
{% endblock %}
